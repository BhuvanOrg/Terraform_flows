name: Orchestration
on:
  workflow_dispatch:
    inputs:
      environment: 
        description: choose the envrionemt
        type: choice
        required: true
        options:
          - tst
          - acc
          - prd
    

jobs:
  start-service:
    runs-on: ubuntu-latest
    steps:
      - name: show vars
        run: echo "Hellpw world"