name: start-services
on: 
  workflow_call:
    inputs:
      environment:
        description: environment varaibles
        type: string
        required: true

jobs:
  start-service:
    runs_on: ubuntu-latest
    environment: ${{ inputs.environment }}

    steps:
      - name: stop
        run: echo "service started"
